# Pin the public npm registry so installs never fall back to a private mirror.
# The project was originally developed on Replit, whose lockfile pinned some
# packages to an internal registry (package-firewall.replit.local) that is
# unreachable outside Replit. This keeps `npm install` working everywhere.
registry=https://registry.npmjs.org/
